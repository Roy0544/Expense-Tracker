export const metadata = {
  title: "Sign-Up - TrackFlow",
  description: "Try Our Expense Tracker Make A Account",
};

export default function DashboardLayout({ children }) {
  return <>{children}</>;
}
