export const metadata = {
  title: "Expenses - TrackFlow",
  description: "Get All Your Expenses In One Place",
};

export default function DashboardLayout({ children }) {
  return <>{children}</>;
}
